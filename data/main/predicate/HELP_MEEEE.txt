Hello,

Okay, so for some reason, when I have a dimension in the predicate, it always causes the predicate to fail if it
is tested on a non-player.

So for example, if I wanted to test if the entity was in the overworld, and I had it in the predicate, it would
always return false. I tested this for all dimensions, to make sure me modifing the dimension type wasn't messing
anything up. But I still had the same issue. I assume this isn't a bug of the game, and that it has something
to do with my lack of knowledge.

Any information would be super super appreciated.